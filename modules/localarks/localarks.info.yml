name: 'Local ARKs Persistent ID Minter'
description: "A minter accompanying the Persistent Identifers module for minting locally managed ARKs resolvable via the N2T (Names to Things) service."
type: module
core: 8.x
core_version_requirement: ^8 || ^9
dependencies:
 - persistent_identifiers
